void main()
{
    int x, y, total, sum, num;

    scanf(x);
    scanf(y);
    total = 0;
    num = 0;
    if (x <= 1)
        sum = y;
    else {
        scanf(x);
        total = x * y;
    }
    printf(total);
    printf(sum);
}
